apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cities.fullname" . }}-test-connection"
  labels:
    {{- include "cities.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cities.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
