apiVersion: v1
kind: Service
metadata:
  name: {{ include "cities.fullname" . }}
  labels:
    {{- include "cities.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cities.selectorLabels" . | nindent 4 }}
